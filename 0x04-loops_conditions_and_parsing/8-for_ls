#!/usr/bin/env bash
# This script displays filenames after the first dash, one per line
for file in *; do
    # Skip hidden files and directories
    if [ -f "$file" ] && [[ "$file" != .* ]]; then
        # Extract part after first dash
        echo "${file#*-}"
    fi
done
